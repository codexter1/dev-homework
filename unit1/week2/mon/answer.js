// datatypes:
// 1. booleans inside of an object
//2. a string inside of an object or an array
//3. an object holding an array with strings or functions
//4. an array either by itself or as part of an object.
//5. an object holding an array of objects with strings
//6. an object holding an array of objects with arrays

let rainbow = ['red','orange','yellow','green','blue','indigo','violet'];

console.log(rainbow[4]);

const tommy = {
  favFood: 'tacos',
  hobby: 'skateboarding',
  hometown: 'San Mo',
  favDataType: 'object',
}

console.log(tommy.hobby);
